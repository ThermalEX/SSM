<Window x:Class="SSM.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SSM Overlay"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Viewbox x:Name="RotatableContent" Stretch="Uniform">
        <Canvas x:Name="OverlayCanvas" Width="1024" Height="600" ClipToBounds="True"/>
    </Viewbox>
</Window>
